* when a convo is clicked via the menu (and subsequently opened in a browser)
  remove it from the menu

* add submenu if more than 5 convos added to menu
	* convo 1
	* convo 2
	* convo 3
	* convo 4
	* see more => 
	
* make Ctrl-V, Ctrl-X, Ctrl-C work in apiKey window

* add sparkle support for updating app

* add preferences for setting update interval

* only send Growl alert if there are true *new* convo updates
	* if the updatedConvos are the same as the old ones, dont notify via Growl again